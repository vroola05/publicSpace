import { DynamicRightDirective } from './dynamic-right.directive';

describe('DynamicRightDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicRightDirective();
    expect(directive).toBeTruthy();
  });
});
