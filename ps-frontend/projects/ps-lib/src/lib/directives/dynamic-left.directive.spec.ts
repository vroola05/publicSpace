import { DynamicLeftDirective } from './dynamic-left.directive';

describe('DynamicLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicLeftDirective();
    expect(directive).toBeTruthy();
  });
});
